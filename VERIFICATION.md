# 검증 결과 — 2026-09-13

- `npm test`: 15/15 통과. 최소 라운드 보호, 30라운드 mock 실행, 구독 환경 격리, 구조화 응답 검증 포함.
- `npm run typecheck` 및 `npm run build`: 통과.
- 로컬 HTTP smoke: 프로젝트 생성, worker 실행, 저장, 보고서 생성 통과. 기본 구독 모드 및 과거 API 모드 요청 차단 확인.
- 실제 로그인된 구독으로 1라운드 전체 실행 성공: GPT 계획 → GPT/Claude 독립 조사 → 양방향 비판 → 양방향 반박 → GPT 최종 보고서. 총 8회 호출 성공, 주장 6개, 보고서 생성 확인.
- 실제 모델: `gpt-6-astra` low / `claude-opus-5` low.
- API 키 환경변수는 자식 프로세스에 전달하지 않으며 API 모드로 자동 전환하지 않습니다.

## 검증 범위와 제한

실제 구독 검증은 1라운드이며 30라운드는 mock으로 검증했습니다. 구독 사용량 제한은 적용됩니다. 제한 또는 오류 시 기록을 보존하고 실패 처리하며 자동 재개하지 않습니다.

CLI가 작성한 출처 URL은 독립적으로 확인된 근거로 승격하지 않습니다. 모델 간 합의도 사실 검증으로 취급하지 않습니다. 출처의 정확성 및 보고서의 내용은 별도 검토가 필요합니다.

`examples/`의 기존 보고서는 mock 실행 예시입니다.

## 참고 자료 첨부 추가 검증

16/16 테스트와 프로덕션 빌드 통과. 첨부 텍스트 저장 및 전체 8단계 전달, 파일 형식·개별 및 전체 길이 제한 검증. 이번 첨부 기능은 mock으로 검증했으며 추가 구독 호출은 하지 않았습니다.

## 프로젝트 구독 사용량 표시

17/17 테스트와 타입 검사 통과. GPT와 Claude의 호출 상태, 모델, 토큰을 프로젝트 기록에서 분리 집계하며 기존 1초 폴링으로 화면을 갱신합니다. 계정 전체 잔여 퍼센트는 두 서비스에 공통인 안정적인 공식 API가 없어 표시하지 않습니다.
